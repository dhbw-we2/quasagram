/**
 * Constants to store login- and register-status in localStorage
 * @type {{REGISTERED: string, LOGIN: string}}
 */
const CONST = {
    REGISTERED: 'user-registered',
    LOGIN: 'user-logged-in'
}
export default CONST